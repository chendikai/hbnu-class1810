<?xml version="1.0" encoding="utf-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/aop
        http://www.springframework.org/schema/aop/spring-aop.xsd
        http://www.springframework.org/schema/tx
        http://www.springframework.org/schema/tx/spring-tx.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- 配置数据库信息 -->
    <bean id="datasource" class="com.mchange.v2.c3p0.ComboPooledDataSource">
        <property name="driverClass" value="com.mysql.cj.jdbc.Driver"></property>
        <property name="jdbcUrl" value="jdbc:mysql:///hbnu?serverTimezone=GMT&amp;useSSL=false&amp;characterEncoding=utf8"></property>
        <property name="user" value="root"></property>
        <property name="password" value="chendikai"></property>
    </bean>

    <!-- 1、配置事务管理器 -->
    <bean id="transcationManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="datasource"></property>
    </bean>

    <!-- 2、配置事务驱动 -->
    <tx:annotation-driven transaction-manager="transcationManager"></tx:annotation-driven>

    <!--
    &lt;!&ndash; 2、配置事务增强 &ndash;&gt;
    <tx:advice id="txAdvice" transaction-manager="transcationManager">
        <tx:attributes>
            &lt;!&ndash; 匹配规则（account*）&ndash;&gt;
            <tx:method name="account"/>
        </tx:attributes>
    </tx:advice>

    &lt;!&ndash; 3、配置切面 &ndash;&gt;
    <aop:config>
        &lt;!&ndash; 配置切入点 &ndash;&gt;
        <aop:pointcut id="pointcut1" expression="execution(* com.hbnu.transcation.UserService.*(..))"></aop:pointcut>

        &lt;!&ndash; 配置切面 &ndash;&gt;
        <aop:advisor advice-ref="txAdvice" pointcut-ref="pointcut1"></aop:advisor>
    </aop:config>
    -->

    <!-- 将数据源注入到jdbcTemplate对象中 -->
    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="datasource"></property>
    </bean>

    <!-- UserDao交给Spring管理，并注入jdbcTemplate对象 -->
    <bean id="userDao" class="com.hbnu.transcation.UserDao">
        <property name="jdbcTemplate" ref="jdbcTemplate"></property>
    </bean>

    <!-- UserService交给Spring管理，并注入userDao对象 -->
    <bean id="userService" class="com.hbnu.transcation.UserService">
        <property name="userDao" ref="userDao"></property>
    </bean>
</beans>